# dependencies
node_modules/
**/node_modules/

# env / secrets — ห้าม commit
.env
.env.local
**/.env
**/.env.local

# builds
dist/
**/dist/

# uploads (dev disk) — prod ใช้ Supabase Storage
**/uploads/

# backups ฐานข้อมูล — มีข้อมูลลูกค้า/ออเดอร์ ห้าม commit เด็ดขาด
backups/
**/backups/

# logs / os
*.log
.DS_Store
